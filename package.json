{
  "name": "maydayos-aviation-emergency-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run server",
    "server": "concurrently \"node server.js\" \"ssh -o StrictHostKeyChecking=no -R 80:localhost:3000 serveo.net\"",
    "local": "node server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "serialport": "^13.0.0",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
